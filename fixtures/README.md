Put `StudyAssigned_Deep_Institution_Intelligence_2026_27_FINAL.xlsx` here for the
Phase 3 import tooling and its tests. Not committed (see `.gitignore`) since it's
real institution data, not a small test fixture.

Small, synthetic workbooks used by unit tests (a handful of rows per sheet) do get
committed here once Phase 3 adds import parsing.
